<%= form_with(model: book) do |form| %>
  <% if book.errors.any? %>
    <div class="error-messages">
      <h2><%= t('views.common.validation_error', errors: i18n_error_count(book.errors.count), name: Book.model_name.human.downcase) %>:</h2>

      <ul>
        <% book.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :title, class: "block-label" %>
    <%= form.text_field :title %>
  </div>

  <div>
    <%= form.label :memo, class: "block-label" %>
    <%= form.text_area :memo %>
  </div>

  <div>
    <%= form.label :author, class: "block-label" %>
    <%= form.text_field :author %>
  </div>

  <div>
    <%= form.label :picture, class: "block-label" %>
    <%= form.file_field :picture %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
