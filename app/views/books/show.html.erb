<p style="color: green"><%= notice %></p>

<div class="show-item">
  <%= render @book %>
</div>

<nav class="page-nav">
  <%= link_to t('books.show.edit'), edit_book_path(@book) %> |
  <%= link_to t('views.common.back', name: t('books.index.page_title')), books_path %>

  <%= button_to t('books.show.destroy'), @book, method: :delete %>
</nav>
